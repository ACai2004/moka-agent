# 创建对话按钮 — 技术对接计划

> 本文档说明如何将 Demo 前端的「☎️ 创建对话」按钮对接技术部的语音通话服务。
> 阅读对象：编码 AI。目标是照此文档可直接编码实现。

---

## 1. 背景与数据流

```
用户完成 Pipeline 后得到 Runtime Prompt
                  │
                  ▼
点击「☎️ 创建对话」按钮
                  │
                  ▼
POST https://voice-dialog.bangbangyouxin.cn/api/v1/sessions
请求体中 runtimePrompt → botConfig.systemPrompt
                  │
                  ▼
返回 { sessionId, callUrl, callToken }
                  │
                  ▼
跳转 https://voice-dialog.bangbangyouxin.cn/h5/call?token=xxx&debug=true
用户进入真实通话
```

---

## 2. 改动范围

仅涉及 **2 个文件**：

| 文件 | 改动量 | 说明 |
|---|---|---|
| `src/main/resources/static/demo/app.js` | ~80 行新增 | HMAC 签名函数 + API 调用逻辑 |
| `src/main/resources/static/demo/index.html` | 1 个 class | 把 `call-btn` 的 `cursor-not-allowed` 改为 `cursor-pointer` |

不需要改动后端代码。前端直接调技术部 API。

---

## 3. 执行步骤概览

| 顺序 | 步骤 | 操作 | 代码量 |
|---|---|---|---|
| **0** | **前置验证** | 用 curl 测试技术部 API 是否可达 | 0 行 |
| 1 | 确认现状 | 检查 `app.js` 中 `el.callBtn` 是否已缓存 | 0 行 |
| 2 | 添加代码 | 在 `app.js` 末尾添加 HMAC 签名 + `createCallAndJump()` | ~80 行 |
| 3 | 接入按钮 | 在 `bindEvents()` 中添加一行点击事件 | 1 行 |
| 4 | 改样式 | `index.html` 中按钮 `cursor-not-allowed` → `cursor-pointer` | 1 个 class |
| 5 | 验证 | 上传小票 → Pipeline 完成 → 点击按钮 → 跳转到通话页 | — |

---

## 4. 前置步骤：先确认技术部 API 可用

在改任何代码之前，先用 curl 测试一下：

```bash
curl -X POST "https://voice-dialog.bangbangyouxin.cn/api/v1/sessions" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: ovd_0rnkOZTbZbHsMcsBhkWpPguKoi82LQkL" \
  -H "Authorization: HMAC-SHA256 test:1:test" \
  -d '{"userId":"curl-test","botConfig":{"systemPrompt":"你好"}}'
```

**期望结果：** 返回 HTTP 4xx（因为签名是假的）说明接口可达。
**如果返回：** `curl: (28) Connection timed out` 或 `Failed to connect` → 接口不通，需要联系技术部。

> 这一步不涉及任何代码改动，纯粹验证网络连通性。

---

## 5. 改动内容

### 5.0 确认现状

确认 `app.js` 中两处已就位：

```javascript
// cacheDom() 中应该有：
el.callBtn = $('call-btn');

// bindEvents() 中 call-btn 此时不应有事件监听
```

如果缺了任何一项，先补上再继续。

### 5.1 在 app.js 末尾添加 API 凭据常量

```javascript
// ============================================================
// 通话对接 — 技术部 API 凭据（demo 环境）
// ============================================================
const CALL_API_KEY = 'ovd_0rnkOZTbZbHsMcsBhkWpPguKoi82LQkL';
const CALL_API_SECRET = '31987887662c00a5277d9ba06b359029088619359487d6a5420a19aa0d157b24';
const CALL_BASE_URL = 'https://voice-dialog.bangbangyouxin.cn';
```

> ⚠️ **安全说明**：这些是 demo 环境凭据，明文放在前端 JS 中在 Demo 阶段可接受。
> 上线前必须将 Key/Secret 迁移到后端配置，前端只调自己后端的代理接口。

### 5.2 在 app.js 末尾添加 HMAC 签名函数

从 `test-call.html` 原样搬过来：

```javascript
// ============================================================
// 通话对接 — HMAC 签名
// ============================================================
async function hmacSha256(secret, message) {
    var encoder = new TextEncoder();
    var keyData = await crypto.subtle.importKey(
        'raw', encoder.encode(secret), { name: 'HMAC', hash: 'SHA-256' }, false, ['sign']
    );
    var signature = await crypto.subtle.sign('HMAC', keyData, encoder.encode(message));
    var bytes = new Uint8Array(signature);
    return btoa(String.fromCharCode.apply(null, bytes));
}

async function createAuthHeader(method, path, jsonBody) {
    var timestamp = String(Date.now());
    var nonce = crypto.randomUUID ? crypto.randomUUID() : timestamp + '-' + Math.random().toString(36).slice(2);
    var stringToSign = method + '\n' + path + '\n' + timestamp + '\n' + nonce + '\n' + (jsonBody || '');
    var signature = await hmacSha256(CALL_API_SECRET, stringToSign);
    return 'HMAC-SHA256 ' + signature + ':' + timestamp + ':' + nonce;
}
```

### 5.3 在 app.js 末尾添加创建通话函数

```javascript
// ============================================================
// 通话对接 — 创建通话 & 跳转
// ============================================================
async function createCallAndJump() {

    // 安全检查：必须有 Runtime Prompt
    if (!state.runtimePrompt || !state.runtimePrompt.finalPrompt) {
        alert('请先完成 Pipeline 解析，再发起通话');
        return;
    }

    // 防重复点击
    const btn = el.callBtn;
    if (btn.disabled) return;
    btn.disabled = true;
    btn.textContent = '⏳ 创建中...';

    const userId = 'demo-' + Date.now().toString(36);
    const url = CALL_BASE_URL + '/api/v1/sessions';

    // redirectUrl：通话结束后跳回 Demo 页面
    const redirectUrl = window.location.origin + '/demo/index.html';

    // 构建请求体（Runtime Prompt 填入 systemPrompt）
    const body = JSON.stringify({
        userId: userId,
        redirectUrl: redirectUrl,
        botConfig: {
            botName: '餐后体验回访 AI',
            systemPrompt: state.runtimePrompt.finalPrompt,
            speakingStyle: '语速适中，语调亲切，像真人客服一样自然',
            speaker: 'sweet-female',
            provider: 'volcano-rtc',
            welcomeMessage: '您好',
            openingLine: '您好，我是餐后体验回访助手，想和您聊聊刚才的用餐体验',
            idleConfig: {
                enabled: true,
                timeoutSeconds: 30,
                maxTriggers: 3,
                resetOn: 'userSpeak'
            },
            endTriggers: [
                {
                    type: 'normal',
                    keywords: ['通话结束', '感谢您的配合'],
                    closingLine: '感谢您的分享，祝您生活愉快！',
                    threshold: 1
                }
            ],
            userPauseTimeoutMs: 1000,
            bargeInEnabled: true
        },
        panelConfig: {
            botDescription: '餐后体验回访',
            welcomeMessage: '您好',
            uiMode: 'minimal',
            theme: 'light',
            acceptLabel: '接听',
            rejectLabel: '挂断',
            features: {
                showChatBubbles: true,
                showAvatar: true,
                showMuteButton: true,
                showMetrics: true,
                showBubbleMetadata: true,
                showSessionStatus: true,
                showDetailsPanel: true
            }
        }
    });

    try {
        // 计算签名
        const authHeader = await createAuthHeader('POST', '/api/v1/sessions', body);

        // 发起请求
        const resp = await fetch(url, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': authHeader,
                'X-API-Key': CALL_API_KEY
            },
            body: body
        });

        if (!resp.ok) {
            const errText = await resp.text();
            throw new Error('HTTP ' + resp.status + ': ' + errText.slice(0, 200));
        }

        const data = await resp.json();

        if (!data.callUrl) {
            throw new Error('返回数据缺少 callUrl');
        }

        // 跳转到通话页面
        const jumpUrl = data.callUrl + '&debug=true';
        window.location.replace(jumpUrl);

    } catch (err) {
        console.error('创建通话失败:', err);
        alert('创建通话失败: ' + err.message);
        btn.disabled = false;
        btn.textContent = '☎️ 创建对话';
    }
}
```

### 5.4 在 bindEvents() 中接入按钮

找到 `app.js` 中的 `bindEvents()` 函数，在末尾加上：

```javascript
    // 通话按钮 — 对接技术部 API
    el.callBtn.addEventListener('click', createCallAndJump);
```

### 5.5 修改 index.html 中按钮样式

找到 `index.html` 中 `#call-btn` 的 class，把 `cursor-not-allowed` 改为 `cursor-pointer`：

```html
<!-- 改前 -->
<button id="call-btn" class="... cursor-not-allowed ...">

<!-- 改后 -->
<button id="call-btn" class="... cursor-pointer ...">
```

---

## 6. 请求体字段说明

| 字段 | 值来源 | 说明 |
|---|---|---|
| `userId` | 随机生成 `demo-xxx` | 技术部用来标识用户 |
| `redirectUrl` | `当前页面 + /demo/index.html` | 通话结束后跳回 Demo 页面 |
| `botConfig.botName` | 固定值「餐后体验回访 AI」 | 通话面板显示的 AI 名称 |
| **`botConfig.systemPrompt`** | **`state.runtimePrompt.finalPrompt`** | **你的 Runtime Prompt，核心对接点** |
| `botConfig.speaker` | `sweet-female` | 音色选择 |
| `botConfig.provider` | `volcano-rtc` | 上游提供商 |
| `botConfig.openingLine` | 固定值 | AI 接通后说的第一句话 |
| `botConfig.bargeInEnabled` | `true` | 允许用户打断 AI |
| `panelConfig.uiMode` | `minimal` | 通话页面 UI 简洁模式 |

---

## 7. 边界情况处理

| 场景 | 处理方式 |
|---|---|
| 用户未完成 Pipeline 就点按钮 | `state.runtimePrompt` 为 null → 弹窗提示「请先完成解析」|
| API 返回 HTTP 4xx/5xx | 显示具体错误信息，恢复按钮可用 |
| 网络断开 | `fetch` 抛出异常 → 提示「网络连接失败」|
| 用户多次点击 | `btn.disabled = true` 防重复，请求结束后恢复 |
| 跨域（CORS）被拦截 | 需要技术部在服务端配置 `Access-Control-Allow-Origin` |
| API Key/Secret 泄露风险 | Demo 阶段可接受明文；上线前需迁移到后端 |

---

## 8. 验证步骤

1. 启动 Demo 服务（`mvn spring-boot:run --moka.llm.mock=false`）
2. 打开 `http://localhost:8080/demo/index.html`
3. 上传小票，等待 Pipeline 完成（约 60-90 秒）
4. 点击「→ AI 推理链路」进入 Page 2
5. 等待 Runtime Prompt 渲染完成
6. 点击 **「☎️ 创建对话」**按钮
7. 验证：浏览器跳转到 `voice-dialog.bangbangyouxin.cn/h5/call?token=xxx` 开始通话

### 如果验证失败

| 现象 | 原因 | 解决 |
|---|---|---|
| 按钮没反应 | `call-btn` 事件没绑上 | 检查 `bindEvents()` 中是否有监听 |
| 弹窗「请先完成解析」| Pipeline 未完成 | 等 Runtime Prompt 出现后再点 |
| 弹窗「HTTP 4xx」| API 签名错误或凭据不对 | 检查 API Key/Secret 是否正确 |
| 白页 / 跳转失败 | CORS 跨域拦截 | 联系技术部开跨域 |
| 网络超时 | 技术部服务不可达 | 先 curl 测试确认服务状况 |

---

## 9. 回滚方案

如果对接后出现问题，只需：

1. 删除 `app.js` 末尾新增的 `createCallAndJump()` 函数、`hmacSha256()`、`createAuthHeader()`、三个常量
2. 在 `bindEvents()` 中删除对应的 `addEventListener` 行
3. 把 `index.html` 中按钮的 `cursor-pointer` 改回 `cursor-not-allowed`

不涉及后端代码，回滚风险极低。
